# Example env file for CT deployment (do NOT commit secrets here)
# Copy this to .env.prod and fill in production values in the deployment environment

# Network & ports
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000
ORCHESTRATOR_PORT=9001
MOCK_TOOL_PORT=9000
OBSERVER_PORT=9002

# Runtime
LOG_LEVEL=info
NODE_ENV=production
RESTART_POLICY=unless-stopped

# Key prefix (do NOT put private keys here)
API_KEY_PREFIX=sk_prod_
