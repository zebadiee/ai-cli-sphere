# Production env file template - keep secrets out of git
# This file should be maintained on the host and not checked into source control.

GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000
ORCHESTRATOR_PORT=9001
MOCK_TOOL_PORT=9000
OBSERVER_PORT=9002

LOG_LEVEL=info
NODE_ENV=production
RESTART_POLICY=unless-stopped

API_KEY_PREFIX=sk_prod_
